{
  "name": "dify-doctor-chatflow-api-test",
  "version": "1.0.0",
  "description": "Dify 医生智能体 Chatflow API 自动化测试项目",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node scripts/run-tests.mjs",
    "test:watch": "vitest",
    "test:verbose": "vitest run --reporter=verbose",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "axios": "^1.7.9",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "vitest": "^2.1.8"
  }
}
